{
  "name": "cron-ticker",
  "version": "1.0.0",
  "description": "Proyecto base para enseñar GitHub Actions en diplomado",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "jest --forceExit",
    "test:coverage": "jest --coverage --forceExit",
    "lint": "eslint app.js tasks/ --ext .js",
    "lint:fix": "eslint src/ tasks/ --ext .js --fix",
    "build": "node scripts/build.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-cron": "3.0.3"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  }
  {
  "name": "ci-pipeline",
  "version": "1.0.0",
  "scripts": {
    "lint": "echo lint ok",
    "test": "echo test ok",
    "build": "echo build ok"
  }
}
}
